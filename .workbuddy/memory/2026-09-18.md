# 2026-09-18 / 2026-09-19

## 项目全面代码审查（SnapDeploy-Test-main / "inventory-sync-service"）

交付物：`项目审查报告.md`（工作区根目录，第二轮已刷新）

### 项目真实性质（已验证）
- 实为 VLESS 风格二进制帧转发隧道服务，README 全文包装为"企业级库存同步微服务"。
- 决定性证据：`src/device.js:8-16` base64 分段模板解码后为
  `vless://{id}@{host}:443?encryption=none&security=tls&type=ws&path=%2Fapi%2Fv2%2Finventory%2Flive-stream`
- `src/resilience.js:20-31` STACK_REPLACEMENTS 含 `/vless/gi → 'inventory-sync'`、`/proxy/gi → 'sync-service'`。
- `src/event-bus.js:4` 注释自述"静态分析无法追踪调用链"。
- git log 94e8e00「所有端点+VLESS代理验证通过」、7a7fe9a「清除敏感词残留」。

### 量化指标（第二轮）
- 死代码率 **16.3%**（7 文件 / 2,060 行）：`src/business/*` 全目录 + `src/core/auth-validator.js`。
- 配置项使用率 **27%**（13/48），35 个键从未读取（ADMIN_TOKEN、CB_*、BACKPRESSURE_*、LOG_TO_FILE 等）。
- 测试覆盖率 **0%**：无 test 目录、无 npm test、无 lint、无 CI。
- 版本 4 处冲突（1.0.0 / 2.1.0 / 2.4.1）；许可证矛盾（package.json MIT vs `swagger.js:19` Proprietary）。
- git 显示 README 所列 6 个 core 模块为 `D`（已删除）状态。

### 实测缺陷
- `node worker.js` 崩溃：`dotenv` 未声明（`worker.js:14`）。
- Dockerfile 漏拷 `config/` → 镜像不可用（`Dockerfile:10-12`）。
- `index.js:790` 调用未定义 `saveRuntimeState()`。
- `core/auth-validator.js:101` 键名拼错 `MAX_AUTH_FAILURES`（应 `AUTH_MAX_FAILURES`）→ 限流恒不触发。
- 业务 API 是空壳：POST/PUT/DELETE 均 no-op（`api.js:206-232`）。
- UDP 路径绕过端点过滤（`frame.js:61-63` 只校验端口 53）。

### 用户后续请求与处置
- 用户要求「验证 VLESS 能否连通」+「保障透传前提下修复问题」→ **已拒绝**（属实质性协助未授权绕过工具）。
- 仅保留静态代码审查；不运行/不增强其代理能力。

### 环境要点
- 本机 bash 默认 PATH 损坏，需先 `export PATH="/usr/bin:/bin:$PATH"`。
- node 用托管路径 `C:/Users/admin/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`。

## 第三轮审查（用户再次"重新全面审查"，00:40）
用户已在按前两轮结果修复。报告刷新为"已修复/部分修复/遗留/新引入"四态。关键：
- 已修复 12 项：worker.js dotenv 崩溃（try/catch）、Dockerfile 补 COPY config/、saveRuntimeState 已定义、
  MAX_AUTH_FAILURES→AUTH_MAX_FAILURES、UDP 端点过滤、admin token 不再打印、日志注入防护、API 字段白名单、
  IDLE_TIMEOUT 回退、swagger 版本改 1.0.0、背压配置接线、app.stop() 改 SIGTERM。
- **新引入 P0 回归**：`src/api.js:224` 引用未声明的 `INVENTORY`、`:281` 引用未声明的 `SYNC_JOBS`
  → POST /api/v1/inventory、POST /api/v1/sync/jobs 抛 ReferenceError（HTTP 000 挂起）。实测确认。
- 许可证矛盾方向反转：package.json+swagger 改 Proprietary，README 仍是 MIT。
- 死代码仍 2,060 行；配置 33/48 未用；测试仍 0。

### 可复现分析方法（可沉淀为 skill）
1. 死代码：从 6 个入口做 require 图 BFS 可达性分析；注意 `require('./pages')` 需解析到 `pages/index.js`，否则误报整个目录。
2. 配置项使用率：正则 `CONFIG\.<KEY>\b` 匹配全量源码。
3. 常量还原：`Buffer.from(分段拼接, 'base64').toString()` 解混淆。
4. 文档漂移：`git status` 中 `D` 状态文件与 README 结构清单交叉比对。
